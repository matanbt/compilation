package TYPES;
/*
    An interface for all the instances-types, e.g. TYPE_CLASS_INSTANCE
 */
public interface I_INSTANCE_TYPE {

    // returns the symbols that is used to create this instances.
    TYPE getSymbolType();
}
